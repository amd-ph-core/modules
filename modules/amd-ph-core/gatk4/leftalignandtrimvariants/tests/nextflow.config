docker.runOptions = '--user default'
process {
    ext.args   = "--split-multi-allelics --dont-trim-alleles --keep-original-ac"
    ext.prefix = { "${meta.id}.normalised" }
    containerOptions = '--read-only'
}
